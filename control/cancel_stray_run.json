{
  "action": "CANCEL_STRAY_RUN",
  "run_id": 32947601660,
  "reason": "Queued Feature Store run still uses workflow snapshot with hard-coded max-parallel 20. Cancel before execution, patch workflow to honor request max_parallel=12, then re-trigger with the same durable run_id and fixed schema v1.0.1."
}
